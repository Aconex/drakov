// @flow 
// var fs = require('fs');

let parseContracts = (fileNames: Array<string>): string => {
    // fs.existsSync(fileNames)

    return fileNames[0];
};

module.exports = {
    parseContracts
}